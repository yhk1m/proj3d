# PLAN.md — 지도 투영법 3D 시각화 웹페이지

> 구현 핸드오프 문서. 위에서부터 순서대로 읽고, 11장의 마일스톤 단위로 구현한다.
> 각 마일스톤은 "완료 기준"을 모두 만족해야 다음으로 넘어간다.
>
> **최우선 규칙: 어떤 도구·모델로 작업하든 PLAN.md 6장의 파이프라인 구조를 유지할 것.** (자세한 내용은 0장)

---

## 0. 작업 순서와 공통 규칙

이 프로젝트는 두 모델을 나눠 쓴다. 구조와 수학은 Claude Code + Claude Fable 5.1이, 시각 연출은 Codex + GPT-6 Astra가 맡는다. 먼저 작업한 쪽이 아키텍처를 결정하므로 **구조를 먼저 만들고, 시각 레이어를 나중에 다듬는다.**

### 0.1 최우선 규칙 — PLAN.md 6장의 파이프라인 구조를 유지할 것

모든 작업자(사람·모델 공통)는 작업을 시작하기 전에 6장을 읽고, 아래를 지킨다.

1. 지도 레이어의 모든 정점(해안선, 경위선, 티소 원, 격자 메시)은 **지리좌표 (λ, φ)로만 보관**한다. 3D 위치를 미리 구워(bake) 저장하지 않는다.
2. 3D 위치는 항상 `B_t( f( R(λ, φ) ) )` 합성으로 계산한다. 이 경로를 우회하는 별도의 위치 계산, 도법별 전용 메시, 도법별 전용 애니메이션 코드를 만들지 않는다.
3. 새 도법은 `projections/registry.js`에 항목을 추가하는 방식으로만 넣는다. 새 조정 과정은 `projections/derivations.js`에 시나리오를 추가하는 방식으로만 넣는다.
4. 씌우기/펼치기는 `t`만, 빛 투영은 `s`만, 수학적 조정은 `f`만 바꾼다. 한 단계의 연출을 위해 다른 단계의 함수를 고치지 않는다.
5. `tests/verify.html`의 전 항목 통과를 항상 유지한다. 통과하지 못하는 변경은 병합하지 않는다.
6. 구조를 바꿔야 할 이유가 생기면 코드를 고치기 전에 이 문서의 6장을 먼저 수정하고, 변경 이유를 `CHANGELOG.md`에 남긴다.

### 0.2 작업 순서

| 단계 | 담당 | 범위 | 브랜치 | 끝나는 조건 |
|---|---|---|---|---|
| A (선택) | Codex + GPT-6 Astra | M1~M2 범위의 **시안**: 지구본, 종이 말기, 내핵 빛의 분위기와 카메라 구도 탐색 | `prototype/look` (병합하지 않음) | 스크린샷과 결정 사항 메모(색, 광선 굵기·밝기, 종이 재질, 카메라 구도) |
| B | Claude Code + Claude Fable 5.1 | M1~M5 본 구현. 6장 구조, 레지스트리, 상태 머신, 클리핑, 검증 페이지 | `main` | M1~M5 완료 기준 충족, `tests/verify.html` 전 항목 통과 |
| C | Codex + GPT-6 Astra | 시각 연출 다듬기: 광선·글로우, 재질·조명, 카메라 연출, 애니메이션 이징, 종이에 새겨지는 효과 | `visual-polish` | 0.3의 범위 준수, 검증 전 항목 통과, 프로젝터 환경 60fps 유지 |
| D | Claude Code + Claude Fable 5.1 | `visual-polish` 검토·병합 후 M6~M7 (나머지 의사원통, 구드, 수업용 마감) | `main` | M6~M7 완료 기준 충족 |

- 단계 A의 코드는 본 코드의 기반으로 쓰지 않는다. 결과물은 "결정 사항 메모"뿐이며, 이 메모를 4장(디자인)과 8장(연출)에 반영한 뒤 단계 B를 시작한다.
- 단계 A를 생략하면 B → C → D 순서로 진행한다.
- M6은 단계 C와 병렬로 진행해도 된다. 이 경우 단계 C는 `scene/`만, M6은 `projections/`·`geometry/`만 건드리므로 충돌이 거의 없다.

### 0.3 단계 C(시각 연출)의 수정 범위

| 구분 | 경로 |
|---|---|
| 수정 가능 | `js/scene/*.js`, `css/`, `js/util/tween.js`, `index.html`의 마크업·스타일 |
| 읽기 전용 | `js/projections/`, `js/geometry/`, `js/state.js`, `tests/` |
| 협의 후 수정 | `js/ui/` (레이아웃과 스타일은 가능, 상태 머신과의 연결 로직은 불가) |

- `scene/` 안에서도 정점 위치 계산은 6장의 합성을 호출해 얻는다. 셰이더로 옮길 때도 같은 합성을 그대로 구현하며, 10장 5번의 방식(두 끝점을 attribute로 전달)을 따른다.
- 시각 효과를 위해 필요한 값(진행도 `t`, `s`, 광원 위치 등)은 `state.js`에서 읽기만 한다.

### 0.4 인수인계 체크리스트 (단계가 바뀔 때마다)

- [ ] `tests/verify.html` 전 항목 통과 화면 캡처
- [ ] 이번 단계에서 바꾼 파일 목록과 요약을 `CHANGELOG.md`에 기록
- [ ] 이 문서의 내용과 코드가 어긋나는 곳이 없는지 확인(어긋나면 문서를 먼저 고친다)
- [ ] 미완료 항목과 알려진 문제를 `CHANGELOG.md`의 "다음 작업자에게" 항목에 기록

### 0.5 두 번째 이후 작업자에게 주는 지시문 (그대로 복사해 첫머리에 붙인다)

```
이 저장소의 PLAN.md를 처음부터 끝까지 읽고 작업한다.
최우선 규칙: PLAN.md 6장의 파이프라인 구조를 유지할 것.
 - 모든 정점은 (λ, φ)로 보관하고, 3D 위치는 B_t(f(R(λ, φ))) 합성으로만 계산한다.
 - 새 도법은 registry.js, 새 조정 과정은 derivations.js에 추가하는 방식으로만 넣는다.
 - tests/verify.html 전 항목 통과를 유지한다. 깨지면 그 변경은 되돌린다.
이번 작업의 범위: (PLAN.md 0.2의 단계와 0.3의 수정 가능 경로를 여기에 적는다)
범위 밖의 파일을 고쳐야 한다고 판단되면 고치지 말고, 이유와 제안을 먼저 보고한다.
```

---

## 1. 목표

고등학교 지리 수업에서 쓸 수 있는 지도 투영법 3D 애니메이션 웹페이지를 만든다.

사용자가 버튼(또는 슬라이더)으로 다음 과정을 단계별로 조작한다.

1. 펼쳐진 종이(직사각형·부채꼴·원판)가 지구본 옆에 놓여 있다.
2. 종이가 말리면서 지구본에 씌워진다(원통·원뿔·평면, 정축·횡축·사축).
3. 지구 내핵에서 빛이 나와 해안선과 경위선이 종이에 투영된다.
4. 지도가 그려진 종이를 다시 펼친다.
5. 빛으로 만들 수 없는 도법(메르카토르, 로빈슨, Equal Earth 등)은 펼친 지도에서 출발해 **수학적 조정 과정**을 단계별 애니메이션으로 보여준다.

### 교육적 원칙 (구현 중 판단이 필요할 때의 기준)

- **메르카토르는 빛 투영이 아니다.** 중심에서 빛을 쏘면 중심원통도법(y = R·tan φ)이 나온다. 메르카토르는 여기서 위선 간격을 정각 조건에 맞게 조정한 결과다. 이 오개념을 바로잡는 흐름("빛 투영 → 고위도 과장 → 간격 조정 → 메르카토르")이 이 페이지의 핵심 서사다. 빛 애니메이션에 "메르카토르"라는 이름을 붙이지 않는다.
- **종이는 늘어나지 않는다.** 말기/펼치기는 길이가 보존되는 등장(isometric) 변형으로 구현한다. 그래야 "가전면(developable surface)" 개념이 전달된다.
- **왜곡은 항상 눈에 보이게 한다.** 티소 지표(Tissot's indicatrix)를 모든 단계에서 켤 수 있어야 한다.
- 모든 단계는 버튼 재생과 슬라이더 스크럽 둘 다 지원한다. 교사가 중간에 멈춰 설명할 수 있어야 한다.

---

## 2. 범위

### v1 포함
- 3장의 투영법 목록 전체
- 5단계 상태 머신, 단계별 한글 자막
- 표준위선 슬라이더, 접선/할선 전환, 정축/횡축/사축 전환, 광원 위치 슬라이더
- 티소 지표, 수식 패널(KaTeX), 간격 함수 그래프, 로빈슨 표 패널
- 도법 비교 모드(외곽선 겹쳐 보기, 그린란드:아프리카 면적비 표시)
- 전체화면/프로젝터 모드, 자동재생, URL 쿼리로 상태 공유

### v1 제외
- 역투영(지도 클릭 → 경위도), 사용자 데이터 업로드, 다국어, 로그인/저장

---

## 3. 투영법 목록

`생성` 열: **빛** = 3단계 광선 투영으로 직접 생성 / **조정** = 다른 도법에서 출발해 5단계에서 변형.

### 3.1 가전면 계열

| id | 이름 | 가전면 | 성질 | 생성 | 비고 |
|---|---|---|---|---|---|
| `centralCylindrical` | 중심원통도법 | 원통 | — | 빛(중심 점광원) | y = tan φ, ±75°에서 클리핑 |
| `lambertCylindricalEA` | 람베르트 정적원통도법 | 원통 | 정적 | 빛(축에서 수평 방사) | y = sin φ |
| `equirectangular` | 등장방형(정거원통)도법 | 원통 | 경선 정거 | 조정 ← centralCylindrical | y = φ |
| `mercator` | 메르카토르 도법 | 원통 | 정각 | 조정 ← centralCylindrical | y = ln tan(π/4 + φ/2), ±85° 클리핑 |
| `miller` | 밀러 도법 | 원통 | 절충 | 조정 ← mercator | y = 1.25·ln tan(π/4 + 0.4φ) |
| `transverseMercator` | 횡축 메르카토르(TM) | 원통(횡축) | 정각 | mercator + aspect 회전 | 우리나라 지형도·UTM 언급 |
| `centralConic` | 중심원추도법 | 원뿔 | — | 빛(중심) | ρ = cot φ₀ − tan(φ − φ₀), θ = nλ |
| `equidistantConic` | 정거원추도법 | 원뿔 | 경선 정거 | 조정 ← centralConic | |
| `lambertConformalConic` | 람베르트 정각원추도법 | 원뿔 | 정각 | 조정 ← centralConic | 표준위선 2개(할선) |
| `albers` | 알베르스 정적원추도법 | 원뿔 | 정적 | 조정 ← centralConic | 표준위선 2개(할선) |
| `gnomonic` | 심사도법 | 평면 | 대권 = 직선 | 빛(중심) | 중심에서 60° 이내 클리핑 |
| `stereographic` | 평사도법 | 평면 | 정각 | 빛(대척점) | |
| `orthographic` | 정사도법 | 평면 | — | 빛(무한원, 평행광) | |
| `azimuthalEquidistant` | 정거방위도법 | 평면 | 중심에서 정거 | 조정 ← stereographic | r = c (각거리) |
| `lambertAzimuthalEA` | 람베르트 정적방위도법 | 평면 | 정적 | 조정 ← stereographic | r = 2·sin(c/2) |

### 3.2 수학적 도법 (가전면 없음)

| id | 이름 | 계열 | 성질 | 조정 출발점 | 핵심 원리 |
|---|---|---|---|---|---|
| `sinusoidal` | 시뉴소이드 도법 | 의사원통 | 정적 | lambertCylindricalEA | 위선을 실제 길이(cos φ)로 축소 |
| `mollweide` | 몰바이데 도법 | 의사원통 | 정적 | lambertCylindricalEA | 2:1 타원 외곽, 보조각 θ |
| `eckert4` | 에케르트 IV 도법 | 의사원통 | 정적 | lambertCylindricalEA | 극을 선으로 표현 |
| `equalEarth` | Equal Earth 도법 | 의사원통 | 정적 | lambertCylindricalEA | 로빈슨의 외형 + 정적성, 다항식 |
| `robinson` | 로빈슨 도법 | 의사원통 | 절충 | equirectangular | 수식이 아닌 표(table)로 정의 |
| `winkelTripel` | 빈켈 트리펠 도법 | 변형 방위 | 절충 | equirectangular + aitoff | 두 도법의 산술평균 |
| `goodeHomolosine` | 구드(호몰로사인) 도법 | 단열 | 정적 | sinusoidal + mollweide | 40°44′ 접합 + 대양 절개 |

UI의 도법 선택기에는 계열별 그룹 외에 **"요즘 세계지도에 쓰이는 도법"** 바로가기 그룹을 둔다: `robinson`, `winkelTripel`, `equalEarth`.

---

## 4. 기술 스택과 제약

- **빌드 없음.** ES 모듈 + import map. `index.html` + `js/` 폴더. GitHub Pages 정적 배포.
- 라이브러리(모두 CDN, 버전은 구현 시점의 최신 안정판으로 **정확히 고정**):
  - `three` (+ `OrbitControls`) — 3D
  - `d3-geo`, `d3-geo-projection` — raw 투영 함수(`geoEqualEarthRaw`, `geoRobinsonRaw`, `geoMollweideRaw`, `geoWinkel3Raw`, `geoAitoffRaw` 등)와 수치 검증 기준
  - `topojson-client`, `world-atlas`(land-110m) — 해안선. JSON은 저장소에 복사해 로컬에서 로드
  - `katex` — 수식
  - `gsap` — 타임라인(선택. 의존성을 줄이려면 `js/util/tween.js` 자체 구현)
- 외부 이미지 텍스처를 쓰지 않는다. 육지 마스크 텍스처는 런타임에 TopoJSON을 오프스크린 canvas(2048×1024, 등장방형)에 그려 생성한다.
- 디자인: 네이비 `#1B2A4A` 기반, Pretendard / Noto Sans KR. 3D 배경은 짙은 네이비, 종이는 미색, 광선은 따뜻한 노랑(가산 블렌딩).

---

## 5. 디렉터리 구조

```
index.html
PLAN.md
CHANGELOG.md               # 단계별 변경 기록, 다음 작업자에게 남기는 메모 (0.4)
css/style.css
data/land-110m.json
js/
  main.js                  # 부트스트랩, 렌더 루프
  state.js                 # 상태 머신 + URL 동기화
  projections/
    registry.js            # 투영법 레지스트리 (6장 스키마)
    perspective.js         # 빛 투영 계열 forward 함수
    adjusted.js            # 조정 계열 forward 함수 (d3 raw 래핑)
    derivations.js         # 수학적 조정 시나리오 (9장)
    robinsonTable.js       # 로빈슨 표 (9.4)
    distortion.js          # h, k, 면적배율, 최대각왜곡 수치 계산
  geometry/
    rotate.js              # aspect 회전 (지리좌표 ↔ 가전면 좌표)
    bend.js                # 종이 등장 변형 B_t (8.2)
    clip.js                # 절개선·도메인 클리핑 (+ 단열 도법 절개 자오선 분할)
    pipeline.js            # 6.1 합성의 유일한 구현: positionOf(ctx, λ', φ') → (X, Y, Z)
    mesh.js                # 격자 위상·삼각형 흩뿌리기·로브 이음매 보정 (three 의존 없음)
  scene/
    globe.js               # 지구본
    camera.js              # 카메라 리그: 단계별 자동 맞춤(3D ↔ 정면 자세), 사용자 조작 시 해제
    paper.js               # 종이 메시
    mapLayer.js            # 지도 레이어(격자 메시 + 선)
    rays.js                # 광원·광선
    tissot.js              # 티소 지표
  ui/
    stepper.js             # 단계 버튼 + 스크러버
    picker.js              # 도법 선택기
    controls.js            # 표준위선·aspect·광원 슬라이더
    sidePanel.js           # 수식, 그래프, 표
    compare.js             # 비교 모드
  util/tween.js
tests/verify.html          # 12장 수치 검증 페이지
```

---

## 6. 핵심 아키텍처

### 6.1 단일 파이프라인

지도 레이어의 모든 정점(해안선, 경위선, 티소 원, 격자 메시)은 지리좌표 (λ, φ)로 저장하고, 매 프레임 다음 합성으로 3D 위치를 구한다.

```
(λ, φ) ──R──▶ (λ', φ') ──f──▶ (x, y) ──B_t──▶ (X, Y, Z)
 지리좌표      가전면 기준       종이 좌표        3D 위치
              (aspect 회전)    (투영 함수)      (종이 굽힘)
```

- `R`: 정축/횡축/사축. 가전면을 돌리는 것이 아니라 좌표를 돌린다. 3D에서는 종이 그룹 전체에 같은 회전의 역을 쿼터니언으로 적용한다.
- `f`: 투영 함수. 5단계(조정)에서는 `f`만 바뀐다.
- `B_t`: 굽힘 함수. 2·4단계(씌우기/펼치기)에서는 `t`만 바뀐다.
- 3단계(빛 투영)의 정점 위치: `lerp( S(λ,φ), B_1(f(R(λ,φ))), s )`. `S`는 구면 위 위치, `s ∈ [0,1]`.
  빛 투영 계열의 `f`는 실제 원근 투영이므로 광원 L, 구면 점 P, 도착점 Q는 자동으로 일직선 위에 있다. 개발 모드에서 공선성(collinearity) assert를 둔다.

### 6.2 레지스트리 스키마

```js
// js/projections/registry.js
export const PROJECTIONS = {
  mercator: {
    id: 'mercator',
    nameKo: '메르카토르 도법',
    family: 'cylindrical',      // cylindrical | conic | azimuthal | pseudocylindrical | modifiedAzimuthal | interrupted
    property: 'conformal',      // conformal | equalArea | equidistant | compromise | none
    surface: { type: 'cylinder' },   // cylinder | cone | plane | null(가전면 없음)
    light: null,                // 빛 투영이면 { type: 'point', d: 0 } 등 (8.3), 아니면 null
    params: { phi0: 0, phi1: null, phi2: null },   // 표준위선 (라디안)
    domain: { phiMin: -85°, phiMax: 85°, maxAngularDist: null },
    forward: (lam, phi, params) => [x, y],   // 단위구(R=1), 라디안
    derivation: 'mercatorFromCentral',       // derivations.js의 시나리오 id, 빛 투영 도법은 null
    captionKo: '…',
  },
  // …
};
```

### 6.3 조정 시나리오 스키마

```js
// js/projections/derivations.js
export const DERIVATIONS = {
  mercatorFromCentral: {
    root: 'centralCylindrical',     // 1~4단계는 root 도법으로 재생
    steps: [
      {
        titleKo: '위선 간격을 정각 조건에 맞게 조정',
        captionKo: '…',
        formulaTex: 'y = \\ln\\tan\\left(\\tfrac{\\pi}{4}+\\tfrac{\\varphi}{2}\\right)',
        morph: { type: 'lerp', to: 'mercator' },   // lerp | equalAreaFamily | custom
        panel: 'spacingGraph',                     // spacingGraph | robinsonTable | blend | lobes | none
      },
    ],
  },
};
```

morph 타입:
- `lerp`: 정점별로 `(1−t)·f_A + t·f_B`.
- `equalAreaFamily`: 9.1 참조. 중간 프레임까지 모두 정적인 모핑.
- `custom`: `(lam, phi, t, prev, params) => [x, y]` 함수를 직접 제공(구드 절개 등). `prev` 는 직전 단계가 끝난 투영.

### 6.4 구현 관례 (단계 B 에서 확정, CHANGELOG 2026-09-21)

- **프레임(가전면 기준 좌표계)**: +Z = 지도 중심 C(λ'=0, φ'=0), +Y = 가전면의 축, +X = Y×Z. 정축 원통은 항등.
  평면(방위)도 접점을 **+Z** 에 두므로 방위 forward 는 d3 raw 와 같은 '적도 중심' 꼴이며, 정축(극)은 C = 북극인 회전으로 만든다.
- **종이 좌표**: 원통 x ∈ [−πr, πr]. 원뿔은 꼭짓점이 (0, ρ_A), ρ_A = 표준위선(할선이면 중간 위선)까지의 거리 — 즉 y = ρ(α) − ρ cos θ 로, d3 의 ρ(0) 관례와 y 오프셋만 다르다(φ₀ → 90°/0° 극한 연속성 때문).
- **원뿔 가족**: 원통 = φ₁ = −φ₀, φ₂ = φ₀ (r = cos φ₀), 평면 = 원뿔의 α = 90° 극한. `geometry/bend.js`의 `coneGeometry` 가 셋을 통일한다. n = sin α < 0.02 이면 원통 공식.
- **도메인**: `{ phiMin, phiMax, maxAngularDist, kind: 'rect' | 'cap' }`. 격자·종이 메시는 kind 에 따라 (λ', φ') 직사각형 또는 접점 중심 (각거리, 방위) 극좌표로 매개화한다. 모핑 중에는 도메인을 선형 보간한다.
- **빛 단계의 정점 진행도**: 빛의 앞머리가 광원 L 에서 같은 속도로 퍼진다고 보고, reach = s·(최대 |Q−L|) 가 |P−L| 을 넘는 순간부터 정점이 P → Q 로 움직인다(정점마다 s_v). 공선성은 유지된다.
- **평면 종이는 펼치기 단계가 없다**: 상태 머신의 타임라인에서 `surface.type === 'plane'` 이면 UNROLLED 전이를 뺀다(이미 펼쳐진 종이). 빛 투영이 끝나면 곧바로 정면 시점.
- **단열 도법의 절개선**: `domain.cuts = { north:[λ…], south:[λ…] }`(라디안). 선은 `clip.splitAtCuts`, 격자·종이는 `mesh.fixLobeSeams` 로 로브 경계를 걸치지 않게 한다. 새 단열 도법도 registry 항목 + derivations 시나리오 + cuts 만으로 넣는다.

---

## 7. 상태 머신

```
FLAT ──wrap──▶ WRAPPED ──project──▶ PROJECTED ──unroll──▶ UNROLLED ──adjust[k]──▶ ADJUSTED(k)
```

- 각 전이는 진행도 `p ∈ [0,1]`를 가지며, 스크러버로 직접 조작 가능. 역방향 재생 가능.
- `surface === null`인 도법을 선택하면 `derivation.root`로 1~4단계를 재생한 뒤 5단계로 이어진다. "조정 단계부터 보기" 버튼으로 UNROLLED로 바로 건너뛸 수 있다.
- UNROLLED 이후에는 카메라가 정면 직교 시점으로 부드럽게 이동하고, 지구본은 좌측 상단에 작게 남긴다.
- URL 쿼리: `?p=equalEarth&stage=adjust&step=1&t=0.6&aspect=normal&phi0=0&tissot=1`

---

## 8. 모듈별 구현 명세

### 8.1 지구본 (`scene/globe.js`)
- 반지름 1. 육지 마스크 텍스처 + 15° 경위선. 3단계에서 불투명도 1 → 0.35로 전환해 내부 광원이 보이게 한다.
- 3단계에서는 종이도 반투명(0.5)으로 한다. 원통·원뿔이 지구본을 감싸면 종이가 불투명할 때 광원·광선이 보이지 않기 때문이다.

### 8.0 카메라 (`scene/camera.js`)
- 단계마다 광원·지구본·종이가 한 화면에 들어오게 자동으로 맞춘다(감쇠 보간). 3D 자세(비스듬한 시선, 상자 투영 맞춤)와 정면 자세(종이 평면 맞춤, 지구본은 좌측 상단 축소)를 펼치기 진행도로 섞는다.
- 사용자가 드래그·휠로 조작하면 자동 추적을 멈춘다. 단계·도법이 바뀌거나 뷰포트의 맞춤 버튼을 누르면 다시 켠다.

### 8.2 종이 굽힘 `B_t` (`geometry/bend.js`)

**원통** (축 = Y축, 반지름 r = cos φ₀; 접선이면 r = 1)
- 종이 좌표 (x, y), x ∈ [−πr, πr]. 곡률 κ = t / r.
- κ < 1e-6: `(x, y, r)`
- 그 외: `( sin(κx)/κ,  y,  r + (cos(κx) − 1)/κ )`
- t = 1에서 `(r·sin(x/r), y, r·cos(x/r))`. 절개선은 뒤쪽(x = ±πr).

**원뿔** (접선 위도 φ₀, 원뿔상수 n = sin φ₀, 꼭짓점 A = (0, 1/sin φ₀, 0))
- 종이 극좌표 (ρ, θ), θ ∈ [−πn, πn]. 반꼭지각 α_t = lerp(90°, φ₀, ease(t)).
- `A + ( ρ·sin α_t·sin(θ / sin α_t),  −ρ·cos α_t,  ρ·sin α_t·cos(θ / sin α_t) )`
- 모든 t에서 길이 보존. t = 0은 꼭짓점 높이의 수평 부채꼴, t = 1은 완성된 원뿔(우산이 접히는 모양).
- 할선 원뿔은 표준위선 2개에서 구와 교차하도록 A와 α를 계산한다.

**평면**: 굽힘 없음. 접점(극 또는 임의 중심)에 접하도록 강체 이동만 한다.

**FLAT 단계 연출**: 종이는 지구본 오른쪽에 떠 있다가 강체 이동으로 접선 위치에 온 뒤 굽힘이 시작된다.

**φ₀ 슬라이더**: 90° → 0°로 움직이면 평면 → 원뿔 → 원통이 연속 변형된다(세 가전면이 한 가족임을 보여주는 연출). n < 0.02 구간은 원통 공식으로 전환한다.

### 8.3 광원과 광선 (`scene/rays.js`)
- 광원 타입:
  - `{ type: 'point', d }`: 축 위 위치 d(지구 중심 기준, 접점 반대 방향이 양수). d = 0 심사/중심원통/중심원추, d = 1 평사, d → ∞ 정사. 방위도법에서는 d 슬라이더를 노출한다(0 ~ 1 + "무한대" 버튼).
  - `{ type: 'axisOrthogonal' }`: 축에서 축과 수직으로 나가는 평행광(람베르트 정적원통).
- 광선은 경위선 15° 교점에서만 그린다(최대 약 300개). `LineSegments` + 가산 블렌딩, s에 따라 L에서 Q 방향으로 자라난다.
- 내핵: 발광 구(반지름 0.08) + `PointLight` + 빌보드 글로우 스프라이트.
- 지도 선이 종이에 "새겨지는" 효과: 도착한 정점부터 선 색을 광선색 → 잉크색으로 전환한다.

### 8.4 지도 레이어 (`scene/mapLayer.js`)
- **격자 메시**: 가전면 좌표 (λ', φ') 2° 격자. 절개선(λ' = ±180°)에서 메시를 분리한다. 커스텀 `ShaderMaterial`을 쓰며, (λ', φ')를 varying으로 넘기고 **프래그먼트 셰이더에서 역회전해** 지리좌표로 육지 마스크를 샘플링한다(횡축·사축에서 UV 이음매가 깨지는 문제 방지).
- **선 레이어**: 해안선 + 경위선. aspect나 도법이 바뀔 때 `geometry/clip.js`로 재분할한다(10장 1번).
- 정점 위치는 CPU에서 매 프레임 계산한다(110m 기준 약 1만 정점). 종이 위에서는 법선 방향으로 0.002 띄우고 `polygonOffset`을 함께 쓴다.
- `domain`을 벗어난 정점이 포함된 선분과 삼각형은 버린다.

### 8.5 티소 지표 (`scene/tissot.js`)
- 위도 −60°~60°, 경도 30° 간격 격자에 각반경 4°의 구면 원(36각형)을 놓는다. 같은 파이프라인을 통과하므로 자동으로 타원이 된다.
- `distortion.js`의 수치 미분으로 h, k, 면적배율 s, 최대각왜곡 ω를 구해, 원에 마우스를 올리면 표시한다. 정적도법이면 "면적배율 1.00" 배지를 띄운다.

### 8.6 측면 패널 (`ui/sidePanel.js`)
- 현재 단계 자막, KaTeX 수식(변하는 항 강조).
- `spacingGraph`: 가로 φ, 세로 y(φ) 또는 r(c). 출발 도법 곡선과 목표 도법 곡선, 현재 t의 곡선을 함께 그린다.
- `robinsonTable`: 9.4의 19행 표. 지도에서 위선에 마우스를 올리면 해당 행을 강조한다.
- `blend`: 빈켈 트리펠용. 두 지도 축소판과 평균 가중치.
- `lobes`: 구드용. 로브 경계와 중앙경선.

### 8.7 비교 모드 (`ui/compare.js`)
- UNROLLED/ADJUSTED 상태에서 다른 도법의 외곽선과 해안선을 반투명하게 겹친다(적도 길이를 맞춰 정규화).
- 그린란드:아프리카 **지도상 면적비**를 폴리곤 면적으로 계산해 표시한다(실제 약 1:14). 메르카토르 / 로빈슨 / Equal Earth 프리셋 버튼을 둔다.

---

## 9. 수학적 조정 시나리오 상세

모든 수식은 단위구(R = 1), 라디안 기준이다.

### 9.1 정적성 보존 모핑 (`equalAreaFamily`)

x가 λ에 비례하는 (의사)원통 정적도법은 모두 다음 꼴이다.

```
y = Y(φ),   x = λ · cos φ / Y'(φ)
```

세로 간격 함수 Y(φ)를 자유롭게 고르면, 면적을 지키기 위한 가로 축척이 자동으로 결정된다는 뜻이다. 따라서

```
Y_t(φ) = (1 − t)·Y_A(φ) + t·Y_B(φ)
x_t    = λ · cos φ / Y_t'(φ)
```

로 모핑하면 **중간 프레임까지 전부 정적도법**이 된다. 모핑 내내 티소 타원의 넓이가 변하지 않는 것을 화면에서 확인할 수 있다.

- Y'(φ)는 중심차분 수치 미분(Δ = 1e-5)으로 구한다.
- 극에서 cos φ / Y'(φ)가 0/0이 되므로 φ를 ±(90° − 1e-4)로 clamp 한다.
- 적용 대상: `lambertCylindricalEA`(Y = sin φ), `sinusoidal`(Y = φ), `mollweide`, `eckert4`, `equalEarth`.

### 9.2 메르카토르 (`mercatorFromCentral`)
1. root = 중심원통. 자막: "빛으로 투영하면 고위도가 끝없이 늘어난다."
2. `lerp` → 메르카토르. 자막: "가로로 늘어난 비율(sec φ)만큼만 세로를 늘리면 모양(각)이 보존된다." 티소 지표가 타원에서 원으로 바뀌는 것을 강조한다. 패널: spacingGraph(tan φ 와 ln tan(π/4 + φ/2)).

### 9.3 Equal Earth (`equalEarthFromLambert`)

정의(Šavrič, Patterson, Jenny, 2018):

```
sin θ = (√3 / 2) · sin φ
y = A1·θ + A2·θ³ + A3·θ⁷ + A4·θ⁹
x = 2√3 · λ · cos θ / ( 3 · (A1 + 3·A2·θ² + 7·A3·θ⁶ + 9·A4·θ⁸) )
A1 = 1.340264,  A2 = −0.081106,  A3 = 0.000893,  A4 = 0.003796
```

구현은 `d3-geo`의 `geoEqualEarthRaw`를 쓰고, 위 식은 패널 표시와 검증에 쓴다.

단계:
1. root = 람베르트 정적원통(축에서 수평으로 나가는 빛). 자막: "면적은 정확하지만 고위도가 납작하게 눌린다."
2. `equalAreaFamily` → Equal Earth. 자막: "눌린 고위도를 세로로 다시 벌린다. 면적을 지키려면 세로로 늘린 만큼 가로를 줄여야 한다: x = λ·cos φ / Y′(φ)." 패널: spacingGraph + "면적배율 1.00 유지" 배지.
3. 로빈슨 외곽선 겹치기. 자막: "로빈슨 도법처럼 친숙한 외형을 목표로 다항식 계수 4개를 정했다. 로빈슨과 달리 면적이 정확하다."
4. 비교 모드 자동 실행: 그린란드:아프리카 면적비를 메르카토르 / 로빈슨 / Equal Earth로 나란히 표시.

### 9.4 로빈슨 (`robinsonFromEquirect`)

정의: `x = 0.8487 · X(φ) · λ`, `y = 1.3523 · Y(φ)` (남반구는 y 부호 반전). 5° 간격 표 사이는 보간한다. 구현은 `d3-geo-projection`의 `geoRobinsonRaw`를 쓴다.

| φ | X (위선 길이 비) | Y (적도로부터 거리 비) | φ | X | Y |
|---|---|---|---|---|---|
| 0° | 1.0000 | 0.0000 | 50° | 0.8679 | 0.6176 |
| 5° | 0.9986 | 0.0620 | 55° | 0.8350 | 0.6769 |
| 10° | 0.9954 | 0.1240 | 60° | 0.7986 | 0.7346 |
| 15° | 0.9900 | 0.1860 | 65° | 0.7597 | 0.7903 |
| 20° | 0.9822 | 0.2480 | 70° | 0.7186 | 0.8435 |
| 25° | 0.9730 | 0.3100 | 75° | 0.6732 | 0.8936 |
| 30° | 0.9600 | 0.3720 | 80° | 0.6213 | 0.9394 |
| 35° | 0.9427 | 0.4340 | 85° | 0.5722 | 0.9761 |
| 40° | 0.9216 | 0.4958 | 90° | 0.5322 | 1.0000 |
| 45° | 0.8962 | 0.5571 | | | |

> 구현 시 이 표를 `d3-geo-projection` 소스의 표와 대조해 검증한 뒤 `robinsonTable.js`에 넣는다.

단계:
1. root = 등장방형(중심원통에서 위선 간격을 균등화한 단계까지 재생, 또는 건너뛰기).
2. `custom`: Y 표 적용. 위선의 세로 위치가 이동한다(40°까지는 거의 등간격, 고위도에서 간격이 좁아짐).
3. `custom`: X 표 적용. 각 위선이 X(φ) 비율로 줄어든다. 극은 점이 아니라 적도 길이의 0.5322배인 선이 된다.
4. 자막: "로빈슨 도법에는 수식이 없다. '보기 좋은' 세계지도를 목표로 시행착오를 거쳐 정한 표가 정의다. 그래서 정각도 정적도 아닌 절충 도법이다." 티소 지표로 확인한다. 패널: robinsonTable.
5. 연혁 자막: 1963년 아서 로빈슨이 랜드 맥널리의 의뢰로 고안, 1988~1998년 내셔널 지오그래픽 협회 세계지도에 사용, 1998년부터는 빈켈 트리펠로 교체.

### 9.5 시뉴소이드 · 몰바이데 · 에케르트 IV
- 모두 root = 람베르트 정적원통, `equalAreaFamily` 모핑.
- 시뉴소이드 자막: "위선 간격을 등간격으로 되돌리면, 각 위선은 실제 길이(cos φ)로 줄어든다."
- 몰바이데: 2θ + sin 2θ = π·sin φ 를 뉴턴법으로 풀고, x = (2√2/π)·λ·cos θ, y = √2·sin θ. 자막: "전체 외곽을 2:1 타원에 맞춘다."

### 9.6 빈켈 트리펠 (`winkelFromBlend`)
1. 정거방위도법(적도 중심)에서 경도를 절반으로 압축해 전 세계를 원 안에 넣는다.
2. 가로를 2배로 늘려 2:1 타원으로 만든다 = 아이토프 도법.
3. 등장방형(표준위선 φ₁ = arccos(2/π) ≈ 50°28′)과 아이토프를 나란히 놓고, 두 좌표의 **산술평균**으로 합친다. 패널: blend.
- 자막: "'트리펠'은 면적·각·거리 세 가지 왜곡을 모두 조금씩 줄였다는 뜻이다."

### 9.7 구드 호몰로사인 (`goodeFromParts`)
1. 시뉴소이드와 몰바이데를 위아래로 나란히 보여준다.
2. 두 도법의 위선 길이가 같아지는 위도 40°44′에서 자른다. 저위도는 시뉴소이드, 고위도는 몰바이데를 접합한다(몰바이데 부분 y 오프셋 0.0528).
3. `custom`: 대양을 따라 찢는다. 로브 정의(경계 / 중앙경선):
   - 북반구: [−180°, −40°] / −100°, [−40°, 180°] / 30°
   - 남반구: [−180°, −100°] / −160°, [−100°, −20°] / −60°, [−20°, 80°] / 20°, [80°, 180°] / 140°
   - 로브별 forward: `x = λ₀ + f(λ − λ₀, φ).x`
   - 모핑: `lerp(비단열 호몰로사인, 단열 호몰로사인)`. 격자 메시는 **로브 단위로 미리 분리**한다.

---

## 10. 난점과 해결 지침

1. **절개선을 가로지르는 선분.** 해안선이 절개선(가전면 좌표 λ' = ±180°)을 넘으면 지도를 가로지르는 긴 줄이 생긴다. aspect가 바뀔 때마다 회전된 좌표계에서 선을 다시 분할해야 한다. `d3.geoEquirectangular().rotate(...)`의 projection stream을 클리핑 도구로 써서 회전과 절개를 한 번에 처리하고, 결과를 다시 라디안 (λ', φ')로 환산해 보관한다. 구드는 로브 경계에서도 같은 분할이 필요하다.
2. **발산.** 중심원통·중심원추·심사·메르카토르는 `domain`으로 클리핑한다. 종이 메시 크기도 같은 범위에 맞춘다.
3. **극의 0/0.** 9.1의 clamp 규칙을 따른다.
4. **z-fighting.** 8.4의 오프셋 규칙을 따른다.
5. **성능.** CPU 갱신으로 시작하고, 60fps를 못 지키면 `B_t`와 `lerp`를 버텍스 셰이더로 옮긴다(도법별 forward는 CPU에서 두 끝점만 미리 계산해 attribute로 전달).
6. **모바일.** 광선 수와 격자 해상도를 절반으로 낮추는 저사양 모드를 둔다. 측면 패널은 하단 시트로 전환한다.

---

## 11. 마일스톤과 완료 기준

### M1 — 지구본 + 원통 말기/펼치기
- [ ] 지구본(육지 마스크, 경위선), OrbitControls, 종이 메시
- [ ] 원통 `B_t` 구현, 스크러버로 t 조작
- [ ] 완료 기준: 임의의 t에서 종이 메시의 가로·세로 변 길이 오차 < 0.1% (`tests/verify.html`에서 자동 확인)

### M2 — 빛 투영
- [ ] 내핵 광원, 광선, 중심원통 투영, 지도 선이 새겨지는 효과
- [ ] PROJECTED → UNROLLED까지 상태 머신 연결
- [ ] 완료 기준: 샘플 점 100개에서 L-P-Q 공선성 오차 < 1e-6. 펼친 결과가 y = tan φ 와 일치

### M3 — 가전면 일반화
- [ ] 원뿔·평면 `B_t`, φ₀ 슬라이더(평면 ↔ 원뿔 ↔ 원통 연속 변형), 접선/할선
- [ ] aspect 회전(정축/횡축/사축), 절개선 재분할, 프래그먼트 셰이더 역회전 샘플링
- [ ] 광원 위치 슬라이더(심사/평사/정사), `axisOrthogonal` 광원
- [ ] 완료 기준: 3.1 표의 "빛" 도법 전부가 `d3-geo` 대응 투영과 오차 < 1e-6. 횡축에서 가로줄 아티팩트 없음

### M4 — 가전면 계열 수학적 조정
- [ ] `lerp` 모핑, 티소 지표, `distortion.js`, 수식 패널, spacingGraph
- [ ] 메르카토르·밀러·등장방형·원추 3종·방위 2종 시나리오
- [ ] 완료 기준: 메르카토르에서 모든 티소 지표의 h/k = 1 ± 0.01. 람베르트 정적원통·알베르스·람베르트 정적방위에서 면적배율 = 1 ± 0.01

### M5 — 현대 세계지도 도법 (우선 구현)
- [ ] `equalAreaFamily` 모핑, Equal Earth 시나리오(9.3)
- [ ] 로빈슨 시나리오(9.4), robinsonTable 패널
- [ ] 빈켈 트리펠 시나리오(9.6), blend 패널
- [ ] 비교 모드(외곽선 겹치기, 그린란드:아프리카 면적비)
- [ ] 완료 기준: Equal Earth 모핑의 t = 0, 0.25, 0.5, 0.75, 1 모든 프레임에서 면적배율 = 1 ± 0.01. 세 도법 모두 `d3` raw 함수와 오차 < 1e-6. Equal Earth에서 면적비가 1:14 ± 5%

### M6 — 나머지 의사원통 + 단열
- [ ] 시뉴소이드, 몰바이데, 에케르트 IV, 구드(로브 분리 메시, 절개 모핑)
- [ ] 완료 기준: 구드 절개 애니메이션 중 로브 경계를 걸치는 삼각형·선분이 없음

### M7 — 수업용 마감
- [ ] 전 단계 한글 자막 검수, 전체화면/프로젝터 모드(큰 글씨, 고대비), 자동재생
- [ ] URL 상태 공유, 키보드 조작(←/→ 단계, 스페이스 재생), 모바일 저사양 모드
- [ ] 완료 기준: 1080p 프로젝터 환경 60fps, 중급 모바일 기기 30fps 이상

---

## 12. 검증 페이지 (`tests/verify.html`)

브라우저에서 열면 다음을 자동 실행하고 통과/실패를 표로 출력한다.

1. 굽힘 등장성: t 11단계 × 도형 3종의 변 길이 보존
2. 빛 투영 공선성
3. forward 함수 vs `d3-geo` / `d3-geo-projection` 대응 투영 (경위도 10° 격자)
4. 정적도법의 면적배율, 정각도법의 h/k
5. `equalAreaFamily` 중간 프레임의 면적배율
6. 로빈슨 표와 `geoRobinsonRaw` 출력의 일치

---

## 13. 배포

- GitHub Pages, bgnl.kr 서브도메인(이름 미정). `CNAME` 파일 포함.
- 모든 CDN 의존성은 정확한 버전으로 고정하고, `data/`의 TopoJSON은 저장소에 포함한다.
